import { NoInformationPipe } from './no-information.pipe';

describe('NoInformationPipe', () => {
  it('create an instance', () => {
    const pipe = new NoInformationPipe();
    expect(pipe).toBeTruthy();
  });
});
